const app=document.querySelector('#app');let token=localStorage.token,user=JSON.parse(localStorage.user||'null'),cfg;const api=async(p,o={})=>{let r=await fetch(p,{...o,headers:{'Content-Type':'application/json',...(token?{Authorization:'Bearer '+token}:{}),...(o.headers||{})}}),x=await r.json();if(!r.ok)throw Error(x.error||'操作失败');return x};const esc=s=>String(s||'').replace(/[&<>]/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[x]));const readFiles=async files=>Promise.all([...files].map(f=>new Promise(ok=>{let r=new FileReader;r.onload=()=>ok(r.result);r.readAsDataURL(f)})));
function logout(){localStorage.clear();token=user=null;login()}function login(){app.innerHTML=`<section class="landing"><div class="landing-hero"><div class="eyebrow">福建农林大学金山学院设计学院 · 建院20周年</div><h1>廿载同心<br><em>青春同行</em></h1><p>一院三地四校区，共庆青春正当时。</p><button id="join">进入打卡</button><a href="#about">了解活动 ↓</a></div><section id="about" class="intro"><div><span class="eyebrow dark">健康赛道</span><h2>食光有约 · 健康同行</h2><p>面向四校区同学的个人健康养成活动。以真实记录、规律三餐为核心，让每一顿饭都成为青春生活的温暖印记。</p></div><div class="data"><b>3</b><span>每日餐食记录</span><b>18</b><span>健康同行日</span><b>4</b><span>校区同步参与</span></div></section><section class="feature-grid"><article><span>01</span><h3>定时打卡</h3><p>早餐、午餐、晚餐分时段开放，系统时间自动核验。</p></article><article><span>02</span><h3>真实记录</h3><p>上传带本人学号的水印截图，留存每一天的健康足迹。</p></article><article><span>03</span><h3>安心参与</h3><p>个人材料仅供工作人员审核，保护你的隐私与日常节奏。</p></article></section><section class="schedule"><span class="eyebrow dark">打卡时段</span><div class="schedule-row"><div><b>早餐</b><strong>06:50 – 10:00</strong></div><div><b>午餐</b><strong>10:30 – 14:00</strong></div><div><b>晚餐</b><strong>16:30 – 19:30</strong></div></div><p>请在各时段内提交餐食水印截图；活动规则以学院正式通知为准。</p></section><footer>廿载同心 · 青春同行　|　福建农林大学金山学院设计学院</footer></section><section class="card login" id="loginCard"><div class="row"><h2>账号登录</h2><button class="secondary right" id="closeLogin">返回宣传页</button></div><p class="muted">使用学号和密码登录。账号由活动管理员创建。</p><form id="login"><label>学号</label><input name="studentId" required><label>密码</label><input name="password" type="password" required><button>登录并打卡</button></form></section>`;document.querySelector('#loginCard').style.display='none';document.querySelector('#join').onclick=()=>{document.querySelector('#loginCard').style.display='block';document.querySelector('#loginCard').scrollIntoView({behavior:'smooth'})};document.querySelector('#closeLogin').onclick=()=>{document.querySelector('#loginCard').style.display='none';window.scrollTo({top:0,behavior:'smooth'})};document.querySelector('#login').onsubmit=async e=>{e.preventDefault();try{let f=new FormData(e.target),x=await api('/api/login',{method:'POST',body:JSON.stringify(Object.fromEntries(f))});token=x.token;user=x.user;cfg=x.config;localStorage.token=token;localStorage.user=JSON.stringify(user);home()}catch(e){alert(e.message)}}}async function home(){let m=await api('/api/me');cfg=m.config;user=m.user;return user.role==='admin'?admin():student(m)}
async function student(m){let x=await api('/api/checkins'),done=Object.fromEntries(x.checkins.map(c=>[c.slotId,c]));app.innerHTML=`<header class="hero"><div class="row"><div><h1>${esc(cfg.activityName)}</h1><div>你好，${esc(user.name)}（${esc(user.studentId)}）</div></div><button class="secondary right" id="out">退出</button></div></header><section class="card"><div class="row"><h2>今日打卡</h2><span class="right muted">${m.date}　系统时间 ${m.time}</span></div><div class="notice">请在规定时段提交含本人学号的水印截图。提交后可在当日时段内重新上传，以最新记录为准。</div><div class="grid" id="slots"></div></section>`;document.querySelector('#out').onclick=logout;let box=document.querySelector('#slots');cfg.slots.forEach(s=>{let c=done[s.id],active=m.time>=s.start&&m.time<=s.end;box.insertAdjacentHTML('beforeend',`<article class="slot ${active?'active':''}"><h2>${s.label}</h2><p>${s.start}–${s.end}</p>${c?`<p class="${c.status==='approved'?'ok':'muted'}">已提交 · ${c.status==='approved'?'已通过':'待审核'}</p>`:`<p class="muted">尚未提交</p>`}<button ${active?'':'disabled'} data-slot="${s.id}">${c?'更新材料':'提交打卡'}</button></article>`)});box.querySelectorAll('button').forEach(b=>b.onclick=()=>form(b.dataset.slot,done[b.dataset.slot]))}
function form(id,c){let s=cfg.slots.find(x=>x.id===id);app.innerHTML=`<header class="hero"><h1>${s.label}打卡</h1><p>${s.start}–${s.end}，请上传水印相机截图。</p></header><section class="card"><form id="send"><label>餐食水印截图（可多选）</label><input required name="photos" type="file" accept="image/*" multiple><label>Elavatine 当日汇总截图（可选）</label><input name="summary" type="file" accept="image/*"><label>备注（可选）</label><textarea name="note" placeholder="如有系统异常或补充说明，请简要填写"></textarea><div class="row"><button type="button" class="secondary" id="back">返回</button><button>上传并提交</button></div></form></section>`;document.querySelector('#back').onclick=home;document.querySelector('#send').onsubmit=async e=>{e.preventDefault();let f=e.target;try{let photos=await readFiles(f.photos.files),summary=f.summary.files[0]?(await readFiles(f.summary.files))[0]:null;await api('/api/checkins',{method:'POST',body:JSON.stringify({slotId:id,photos,summary,note:f.note.value})});alert('提交成功');home()}catch(e){alert(e.message)}}}
async function admin(){let date=new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Shanghai'}),x=await api('/api/admin/dashboard?date='+date),slots=cfg.slots;let rows=x.students.map(s=>`<tr><td>${esc(s.name)}<br><small>${esc(s.studentId)}</small></td><td>${esc(s.campus||'—')}</td>${s.slots.map(c=>`<td>${c?`<span class="pill ${c.status==='approved'?'done':'pending'}">${c.status==='approved'?'通过':'已交'}</span><br><button class="secondary review" data-id="${c.id}">查看</button>`:'<span class="muted">未交</span>'}</td>`).join('')}</tr>`).join('');app.innerHTML=`<header class="hero"><div class="row"><div><h1>活动管理后台</h1><div>${esc(cfg.activityName)}</div></div><button class="secondary right" id="out">退出</button></div></header><section class="card"><div class="row"><h2>今日提交总览</h2><label class="right">日期 <input id="date" type="date" value="${x.date}"></label><button class="secondary" id="reload">查询</button></div><p class="muted">共 ${x.students.length} 名学生。点击“查看”可核对截图并审核。</p><div style="overflow:auto"><table><thead><tr><th>学生</th><th>校区</th>${slots.map(s=>`<th>${s.label}<br><small>${s.start}–${s.end}</small></th>`).join('')}</tr></thead><tbody>${rows||'<tr><td colspan="6">尚未创建学生账号</td></tr>'}</tbody></table></div></section><section class="grid"><div class="card"><h2>创建学生账号</h2><form id="add"><label>姓名</label><input name="name" required><label>学号</label><input name="studentId" required><label>校区</label><input name="campus" placeholder="如：旗山"><label>初始密码</label><input name="password" required><button>创建账号</button></form></div><div class="card"><h2>打卡时段设置</h2><form id="settings"><label>活动名称</label><input name="activityName" value="${esc(cfg.activityName)}">${slots.map(s=>`<div class="row"><label style="width:55px">${s.label}</label><input name="${s.id}Start" type="time" value="${s.start}"><span>至</span><input name="${s.id}End" type="time" value="${s.end}"></div>`).join('')}<button>保存设置</button></form></div></section>`;document.querySelector('#out').onclick=logout;document.querySelector('#reload').onclick=()=>adminFor(document.querySelector('#date').value);document.querySelector('#add').onsubmit=async e=>{e.preventDefault();try{await api('/api/admin/users',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});alert('已创建');admin()}catch(e){alert(e.message)}};document.querySelector('#settings').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target),slots=cfg.slots.map(s=>({...s,start:f.get(s.id+'Start'),end:f.get(s.id+'End')}));try{await api('/api/admin/config',{method:'PUT',body:JSON.stringify({activityName:f.get('activityName'),slots})});alert('已保存');home()}catch(e){alert(e.message)}};document.querySelectorAll('.review').forEach(b=>b.onclick=()=>review(x.students,b.dataset.id))}
async function adminFor(d){let x=await api('/api/admin/dashboard?date='+d);cfg=x.config;let original=Date.prototype.toLocaleDateString;Date.prototype.toLocaleDateString=function(){return d};await admin();Date.prototype.toLocaleDateString=original}
function review(students,id){let c;for(let s of students){c=s.slots.find(x=>x&&x.id===id);if(c)break}app.insertAdjacentHTML('beforeend',`<section class="card" id="review"><h2>审核材料</h2><div class="photos">${c.photos.map(p=>`<a href="${p}" target="_blank"><img src="${p}"></a>`).join('')}${c.summary?`<a href="${c.summary}" target="_blank"><img src="${c.summary}"></a>`:''}</div><p>${esc(c.note||'无备注')}</p><button id="yes">通过</button> <button class="secondary" id="no">驳回</button> <button class="secondary" id="close">关闭</button></section>`);document.querySelector('#close').onclick=()=>document.querySelector('#review').remove();document.querySelector('#yes').onclick=()=>approve('approved');document.querySelector('#no').onclick=()=>approve('rejected');async function approve(status){try{await api('/api/admin/checkins/'+id,{method:'PUT',body:JSON.stringify({status})});alert('已审核');admin()}catch(e){alert(e.message)}}}
if(token)home().catch(logout);else login();
