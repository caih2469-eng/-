# 全站性能与安全回归验收报告

## 1. 验收范围

- 分支：`fix/full-performance-20260729`
- 基线提交：`8dae20b0c27fa756cca23a8a5bc50b4bd8683942`
- 当前验收工作树：本 Draft PR 最新工作树（提交号见 PR）
- 测试站：<https://jinshan20-test.pages.dev>
- Cloudflare 资源：测试 Pages、测试 D1 `jinshan20-test`、测试 R2 `jinshan20-test`
- 正式站、正式 D1、正式 R2：本轮未修改、未压测、未部署

本轮覆盖请求治理、登录启动链路、学生首页聚合、打卡幂等快速提交、任务和历史记录批量查询、广场与排行榜缓存、R2 直传确认、多图上传、静态资源缓存和请求可观测性。

## 2. 自动化回归

| 项目 | 结果 |
| --- | --- |
| Node 自动化测试 | 59/59 通过 |
| 仓库 UTF-8 与乱码扫描 | 通过；所有纳入扫描的文本均可严格按 UTF-8 解码，0 项违规 |
| URL 规范化 | 通过；业务生成地址不会出现 `//api/` |
| 入口页隔离 | 通过；入口页只请求 `entrance.js`，未请求 `app.js` |
| 手机、平板、电脑和微信 UA 布局模拟 | 通过；未检测到横向溢出 |
| 登录与管理员越权 | 通过 |
| 私有图片匿名访问 | 403、`Cache-Control: no-store` |
| 公开图片缺失 | 404、`Cache-Control: no-store` |
| 图片 HEAD | 200、响应体为 0 字节 |

自动化测试中的 D1 故障日志为故障注入用例，测试本身通过。现有
`MODULE_TYPELESS_PACKAGE_JSON` 警告不影响测试结果。

## 3. 真实 Cloudflare 700 用户测试

测试模型使用测试 D1 中 700 个专用账号，客户端并发 100，请求超时 30 秒。

| 场景 | 成功/总数 | 错误率 | P50 | P95 | P99 | 最大 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 集中登录 | 700/700 | 0% | 688ms | 1,397ms | 1,658ms | 2,231ms |
| 鉴权读取（`/api/me`、`/api/tasks`） | 700/700 | 0% | 944ms | 1,238ms | 1,462ms | 2,080ms |
| 排行榜读取 | 700/700 | 0% | 218ms | 458ms | 655ms | 1,377ms |

吞吐量分别为 70.98、68.35 和 167.00 请求/秒。登录 P95 已低于 5 秒目标。

说明：这是从当前开发机到真实 Cloudflare 测试环境的外部压测，不是生产流量回放。700 个 `perf-load-*` 测试账号保留在测试数据库，便于后续重复验收；正式数据库没有写入这些账号。

## 4. R2 直传与确认

使用测试 R2 中 32,034 字节 WebP 样本，20 组上传流程，并发 5：

| 阶段 | P50 | P95 | 最大 |
| --- | ---: | ---: | ---: |
| 登录 | 353ms | 808ms | 808ms |
| 申请上传地址 | 443ms | 507ms | 507ms |
| 浏览器直传 R2 | 969ms | 2,112ms | 2,112ms |
| D1 确认 | 937ms | 1,201ms | 1,201ms |
| 完整工作流 | 2,754ms | 4,553ms | 4,553ms |

20/20 成功，错误率 0%。测试产生的 20 个 R2 对象、`media_objects` 和
`media_upload_intents` 记录已清理。

本轮没有再次执行 700 张、约 3.5GB 的瞬时上传；免费额度和实际活动模型下，应使用分批上传模型，不能把该项写成已通过。

## 5. 图片权限与缓存

### 5.1 公开图片

测试 URL 使用稳定图片 ID、`variant=thumb` 和版本参数。

| 请求 | 状态 | 缓存 | 大小 | TTFB | 总耗时 | Server-Timing |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| 第 1 次 | 200 | MISS | 32,034B | 976ms | 1,003ms | D1 101ms、R2 171ms |
| 第 2 次 | 200 | HIT | 32,034B | 685ms | 706ms | D1 97ms、Cache 0ms |
| 第 3 次 | 200 | HIT | 32,034B | 334ms | 601ms | D1 100ms、Cache 0ms |

响应包含：

- `Content-Type: image/webp`
- `Content-Length: 32034`
- `ETag`
- `Content-Disposition: inline`
- `X-Content-Type-Options: nosniff`
- `Cache-Control: public, max-age=31536000, s-maxage=31536000, immutable`
- `X-Image-Cache: MISS/HIT`

第二、第三次请求未再次读取 R2。Cloudflare 同时返回 `CF-Cache-Status: HIT`。

### 5.2 私有图片

| 场景 | 状态 | Content-Type | Cache-Control |
| --- | ---: | --- | --- |
| 未登录访问管理员签名 URL | 403 | JSON | `no-store` |
| 登录管理员首次访问 | 200 | `image/webp` | `private, max-age=900` |
| 登录管理员第二次访问 | 200 | `image/webp` | `private, max-age=900` |

私有图片没有进入共享 Cache API，也没有 `CF-Cache-Status`。首次 150,922 字节图片总耗时 2,193ms，第二次为 979ms。

### 5.3 历史广场缩略图补全

本轮只对测试 D1 `jinshan20-test` 和测试 R2 `jinshan20-test` 执行。全量查询测试广场帖子关联的图片后，发现并补全 1 张缺失缩略图：

| 媒体 ID | 帖子 ID | 原图大小 | 原图对象 | 补全缩略图 |
| --- | --- | ---: | --- | --- |
| `c4d96fa5-fd0c-42be-9e4d-000d75ed117e` | `f9d741dd-3a31-494a-9865-ee66d79e970a` | 465,730B | `task-submissions/64116bd5-9fde-4da7-b3d5-2a2976fbaeff/45e38759-947b-4e31-9dfe-a7cd8df6f7c7/c4d96fa5-fd0c-42be-9e4d-000d75ed117e.jpg` | 27,742B WebP，360×203 |

执行统计：

- 执行前缺失：1；
- 补全成功：1；
- 失败：0；
- R2 新增对象：1；
- D1 新增 `image_variants` 记录：1；
- 执行后缺失：0；
- 第二次重复执行：新增对象 0、新增记录 0，证明幂等。

网络文件大小从 465,730B 降至 27,742B，减少约 94.0%。测试站同一缩略图连续请求均为 `image/webp`，并返回 `X-Image-Cache: HIT`。广场接口和前端卡片均优先使用 `variant=thumb`；服务端仍保留 `display` 回退用于兼容尚未迁移的环境，但存在回退即不得判定缩略图验收通过。

## 6. 浏览器性能测试

以下数据来自 Chrome DevTools 对真实 Cloudflare 测试站的实验室模拟，不等同于真实微信真机结论。

### 6.1 入口页

条件：Fast 4G、4 倍 CPU 降速、手机触控视口。

| 环境 | LCP | TTFB | CLS | 网络请求 |
| --- | ---: | ---: | ---: | ---: |
| iOS 微信 UA 模拟 | 1,098ms | 103ms | 0.00 | 4 |
| Android 微信 WebView UA 模拟 | 1,081ms | 190ms | 0.00 | 4 |

入口页请求仅包含页面、WOFF2 字体、`entrance.js` 和 favicon，未加载
`app.js`。

### 6.2 登录和首页

- 登录按钮交互 INP：49ms。
- 登录后的首页 LCP：2,040ms；TTFB 130ms；CLS 0.00。
- 已登录二次加载：`/api/session` 434.4ms、页面渲染 8.2ms、完整启动 491.6ms。
- 首页仅用一个 `/api/session` 聚合请求恢复会话和学生首屏，不再串行请求首页、任务、历史和排行榜。
- 当前压缩传输：`bootstrap.js` 1,544B、`style.css` 14,288B、`app.js` 39,954B。

### 6.3 广场和图片查看

- 广场第一页 SQL 分页上限 20；当前测试数据为 2 条。
- 打开广场新增 1 个列表 API 和 2 个可视图片请求。
- 点击详情图片后 56ms 内打开查看器并显示已加载缩略图。
- 首次 1,280px 展示图网络耗时 908ms。
- 缓存后再次打开：查看器首帧 4ms，展示图替换 10ms。
- 关闭查看器后仍保留广场详情、已展开状态和滚动位置。

## 7. D1 查询计划

- 登录：`idx_users_student_id`，按学号索引查找并 `LIMIT 1`。
- 公开图片：图片主键、提交主键、`plaza_posts.submission_id` 唯一索引以及
  `image_variants` 复合主键均被使用。
- 广场：`idx_plaza_posts_status_published`、点赞、浏览、评论和提交图片索引均被使用；查询在 SQL 层执行 `LIMIT 20 OFFSET`。
- 未发现需要额外重复创建的主键索引。

## 8. 修改前后对比

| 项目 | 基线/旧实现 | 当前实现 |
| --- | --- | --- |
| 登录后初始化 | 多个接口串行/重复加载 | 单个 `/api/session` 聚合恢复，框架优先 |
| 700 登录本地旧基线 | P95 约 16.8s | 真实 Cloudflare 测试 P95 1.397s |
| 广场列表 | 页面状态变化可能重复请求 | 20 条 SQL 分页、视图缓存、局部更新 |
| 图片打开 | 点击后才等待大图 | 立即显示当前缩略图，异步替换展示图 |
| 公开图片 | 每次读取 R2 | 首次 MISS，后续边缘 HIT |
| 多图上传 | 串行、失败影响整批 | 独立上传会话、1/2 并发、仅重试失败项 |
| 动态内容处理 | 全局观察整个 `#app` | 渲染完成后只处理指定容器 |

本地旧基线和真实 Cloudflare 数据不是完全相同环境，只用于说明数量级变化，不能当作严格 A/B。

## 9. 尚未满足的验收项

1. **真实 Android/iPhone 微信、QQ、Chrome/Safari 设备验收：未通过。**
   当前只有 UA、触控视口、网络和 CPU 降速模拟，没有真实设备记录，因此不能写“真机通过”。
2. **正式 D1/R2 的历史缩略图库存未核验：未通过。**
   本轮按要求只在测试资源完成全量核验和补全，没有读取或操作正式 D1/R2。
   在用户明确授权正式执行前，不能宣称正式环境历史缩略图全部达标。
3. **任意外部输入的双斜杠 URL 仍可能由 Pages 静态路由返回 SPA：未通过。**
   业务代码生成的 URL 已统一并有测试保障，不会生成 `//api/`。
4. **3.5GB 图片上传压测和生产环境压力测试：未通过。**
   本轮没有执行，也没有操作生产资源。

仓库与测试数据中文乱码阻塞项已通过；测试环境历史缩略图阻塞项已通过。当前 PR 必须继续保持 Draft，在上述“未通过”项关闭前不得合并或部署正式站。

## 10. 回滚

### 测试站

测试站可在 Cloudflare Pages 部署历史中回滚到本轮前的生产部署：

- 部署 ID：`22cbcee8-169a-4c06-bbdc-e1a5e5216f84`
- 来源分支：`develop`
- 来源提交：`f449ebf`

### 代码

按以下顺序反向还原本轮提交，或关闭草稿 PR：

1. `c58802b`
2. `ff4fb20`
3. `39ffac7`
4. `44eb8a0`
5. `59e3a43`
6. `cba2e7f`
7. `251565c`

正式站未部署本轮代码，因此正式环境当前无需回滚。

## 11. 2026-07-30 第2、3、4项复验

本节数据来自本次实际测试环境执行。正式 D1、正式 R2 和正式站均未读取、写入或部署。

### 11.1 历史广场缩略图审计

- 新增只读脚本：`scripts/audit-plaza-thumbnails.mjs`。
- 脚本允许显式选择 `production`，但代码路径只允许 D1 `SELECT/WITH` 与 R2 对象读取。
- 正式资源未获得授权，因此正式库存状态仍为：**未通过**。
- 测试环境实际审计：广场媒体 2 个，合规 0 个，受影响 2 个。
- 测试环境问题：最长边超过 360px 1 个；缺少 display 对象或记录 1 个。
- 测试环境没有发现：thumb D1 记录缺失、thumb R2 对象缺失、非 WebP、超过 120KB、原始对象缺失、错误对象键。
- 正式 backfill 必须同时提供：
  `--environment production --apply --confirm-production jinshan20`。
- 缺少任何一个参数都会拒绝；异常已有对象不会被覆盖；原图不会被修改或删除。

### 11.2 Pages 双斜杠路由

测试站实际请求：

`https://jinshan20-test.pages.dev//api/me?probe=double-slash`

结果：

- 第一次响应：HTTP 308。
- `Location`：`https://jinshan20-test.pages.dev/api/me?probe=double-slash`。
- 跟随跳转后：HTTP 401。
- `Content-Type`：`application/json; charset=utf-8`。
- 响应体为登录状态 JSON，不是 `text/html` SPA。
- 查询参数完整保留，跳转目标固定为当前同源站点。

### 11.3 700个实际业务上传

旧 `production-upload-load.mjs` 已停用，不再作为验收工具。3.5GB 伪造负载条件已删除，
由 700 个独立测试账号的真实业务链路替代：

`login → member-checkin-fast → member-checkin PUT → history query verification`

| 并发 | 用户 | 成功 | 失败 | 成功率 | 5xx | Fast上传平均 | Fast上传P95 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 10 | 175 | 175 | 0 | 100% | 0 | 2031.7ms | 2410.2ms |
| 25 | 175 | 175 | 0 | 100% | 0 | 1987.1ms | 2395.0ms |
| 50 | 175 | 175 | 0 | 100% | 0 | 2073.3ms | 2531.1ms |
| 100 | 175 | 175 | 0 | 100% | 0 | 2537.2ms | 3003.9ms |

验收结果：

- 700/700 成功，成功率 100%。
- 5xx 为 0，5xx 率 0%。
- 重复幂等键返回同一媒体记录；R2 和 D1 数量均未增加。
- 重复提交 0；重复媒体对象键 0。
- R2 对象 700，等于成功上传数。
- thumb 对象 0。
- 测试结束后，用户、管理员、队伍、任务、上传会话、媒体记录、打卡记录和 R2 对象均为 0。
- 写入压测接口已恢复关闭；测试站最终配置 `ALLOW_LOAD_TESTS=false`。

真机验收本轮按用户要求暂不处理，不能标记为通过。PR继续保持 Draft，不合并 `main`，不部署正式站。
